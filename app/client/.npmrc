# Use node_modules structure instead of global store
node-linker=hoisted

# Store location (optional - pnpm will use default)
# If you want to ensure it's not created in project root
shamefully-hoist=false
